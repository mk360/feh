// import Button from "../objects/button";
// import MainScene from "../scenes/mainScene";

// function createEnemyRangeButton(scene: MainScene) {
//     let enabled = false;
//     const button = new Button(scene, "Enemy Range").setName("enemy-range");
//     button.on("pointerup", () => {
//         enabled = !enabled;
//         scene.processAction({
//             type: "display-enemy-range",
//             enabled,
//         });
//     });

//     return button;
// };

// export default createEnemyRangeButton;
