import { Time } from "phaser";
import Hero from "../../objects/hero";
import MainScene from "../../scenes/mainScene";

function DrawBack(scene: MainScene, assisting: Hero, assisted: Hero, sourceHeroCoordinates: string, targetHeroCoordinates: string) {
    // const timelineData: Time.Timeline
    return [];
};

export default DrawBack;
