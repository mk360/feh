import Hero from "../../objects/hero";
import MainScene from "../../scenes/mainScene";

function SmiteAssist(scene: MainScene, assisting: Hero, assisted: Hero, sourceHeroCoordinates: string, targetHeroCoordinates: string) {
    return [];
};

export default SmiteAssist;
