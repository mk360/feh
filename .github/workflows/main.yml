name: CI

on: [push, pull_request]

permissions:
  pages: write
  id-token: write

jobs:
  build:
    environment:
      name: master
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install

      - name: Build Packages
        run: npm run build

      - name: Check Credentials
        run: npx gh-pages -u "$(git --no-pager log --format=format:'%an' -n 1)" <"$(git --no-pager log --format=format:'%ae' -n 1)">

      - name: Deploy to GitHub Pages
        run: npx gh-pages -d dist -b master
